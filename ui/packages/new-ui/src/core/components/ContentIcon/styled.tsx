import styled from 'styled-components';

const Wrapper = styled.div`
  display: flex;
  flex-direction: row;
`;

const Content = styled.div`
  margin-left: 20px;
`;

export default {
  Wrapper,
  Content
};
