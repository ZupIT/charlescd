import { COLOR_OBSERVATORY } from 'core/assets/colors';
import { COLOR_WHITE } from 'core/assets/colors';

export const light = {};

export const dark = {
  background: COLOR_OBSERVATORY,
  button: COLOR_WHITE
};
