const {{ camelCase name }}IntialState = {
  loading: {
    list: false,
  },
  list: {
    page: 0,
    content: [],
    last: false,
  },
  item: null,
}

export const {{ camelCase name }}Types = {
  toggleListLoading: '{{constantCase namee}}/TOGGLE_LOADING',
}

export const {{ camelCase name }}Actions = {
  toggleListLoading: () => ({ type: {{ camelCase name }}Types.toggleListLoading }),
}

export const {{ camelCase name }}Reducer = (state = {{ camelCase name }}IntialState, action) => {
  switch (action?.type) {
    case {{ camelCase name }}Types.toggleListLoading:
      return {
        ...state,
        loading: {
          ...state.loading,
          list: !state.loading,
        },
      }
    default:
      return state
  }
}
